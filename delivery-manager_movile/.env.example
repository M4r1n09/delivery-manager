# Database Configuration
DB_HOST=localhost
DB_NAME=ice_delivery
DB_USER=postgres
DB_PASSWORD=password
DB_PORT=5432

# You can copy this file to .env and modify the values according to your PostgreSQL setup
